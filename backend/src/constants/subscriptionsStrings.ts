/**
 * * 📦 Order Event Constants
 * ? These constants represent event names for order-related actions.
 * ? Useful when working with event emitters, message queues, or WebSocket notifications.
 */

// 🆕 Event triggered when a new order is created
export const ORDER_CREATED = "ORDER_CREATED";

// 🔄 Event triggered when an existing order is updated
export const ORDER_UPDATED = "ORDER_UPDATED";